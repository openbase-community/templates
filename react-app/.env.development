# Development environment - GA disabled
VITE_GA_TRACKING_ID=

# Leave VITE_API_BASE_URL empty to call the API through Vite's same-origin proxy.
# Use VITE_API_PROXY_TARGET to point the local proxy at a different backend port.
VITE_API_PROXY_TARGET=http://127.0.0.1:8000
