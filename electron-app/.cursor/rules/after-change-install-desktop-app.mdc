---
description: Always rebuild and install local desktop app after changes
globs:
alwaysApply: true
---

After every change in this repository, run `npm run install-local-app` before handing off.

If the command fails, report the failure and likely cause. Do not skip this step silently.
