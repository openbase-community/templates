[project]
name = "$${name_kebab}"
version = "0.1.0"
description = "$${description}"
readme = "README.md"
requires-python = ">=$${python_version}"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.25.0",
    "pydantic>=2.12.5",
]

[project.scripts]
$${name_kebab} = "$${name_snake}:main"
