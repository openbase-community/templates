{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "MCP Dev: $${name_snake}.py",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/run_mcp_dev.py",
            "console": "integratedTerminal",
            "justMyCode": false,
            "envFile": "${workspaceFolder}/.env",
            "python": "${command:python.interpreterPath}"
        }
    ]
}
